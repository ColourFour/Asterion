# Repo Size Cleanup - 2026-06-15

## Summary

- Starting repo size: `1.0G` from `du -sh .`.
- Ending repo size after cleanup and verification: `1.0G` from `du -sh .`; `1,068,688K` from `du -sk .` after the required build/static verification regenerated `docs/` output and ignored TypeScript build-info files were removed again.
- Estimated size removed directly: about `376K` of ignored generated cache files.
- Net visible size saved at `du -sh` scale: none.
- No clearly unused legacy game/art asset directories were present in the current working tree.

## Commands Run

- `du -sh .`
- `du -h -d 1 . | sort -h`
- `find . -type f -not -path './.git/*' -exec du -h {} + | sort -h | tail -100`
- `du -h -d 2 docs 2>/dev/null | sort -h | tail -50`
- `du -h -d 2 public 2>/dev/null | sort -h | tail -50`
- `du -h -d 2 reports 2>/dev/null | sort -h | tail -50`
- `du -h -d 2 asset-manifests 2>/dev/null | sort -h | tail -50`
- `find . \( -iname '*avatar*' -o -iname '*badge*' -o -iname '*rank*' -o -iname '*trophy*' -o -iname '*xp*' -o -iname '*game*' -o -iname '*guardian*' -o -iname '*lore*' -o -iname '*quest*' -o -iname '*mission*' -o -iname '*arena*' -o -iname '*character*' -o -iname '*sprite*' -o -iname '*background*' -o -iname '*mockup*' -o -iname '*screenshot*' \) -not -path './.git/*' -not -path './node_modules/*'`
- `grep -RIn "avatar\|badge\|rank\|trophy\|xp\|game\|guardian\|lore\|quest\|mission\|arena\|character\|sprite" src public docs 2>/dev/null`
- `git ls-files`
- `git status --short`
- `git ls-files -z | xargs -0 du -h 2>/dev/null | sort -h | tail -100`
- `npm test`
- `npm run build`
- `npm run static:check`

`du -d` is supported on this machine, so no local equivalent was needed.

## Top Size Sources Before Cleanup

- `public/`: `471M`
- `.git/`: `292M`
- `node_modules/`: `113M`
- `docs/`: `112M` before the required build; `127M` after build regenerated static output
- `content-model/`: `36M`
- `tools/`: `2.2M` before cleanup
- `src/`: `844K`

Largest individual files before cleanup were exam-bank JSON/images, `content-model` PDFs, and dependency binaries under `node_modules`. The largest non-dependency local files included:

- `public/assets/exam-bank-data/asterion_exam_bank_catalog_v1.json`: `38M`
- `public/assets/exam-bank-data/question_bank.json`: `16M`
- `public/assets/exam-bank-data/asterion_content_lab_candidates_v1.json`: `11M`
- `content-model/P1/p1-content map.pdf`: `7.9M`
- `content-model/M1/m1-total.pdf`: `5.0M`
- `content-model/P3/integral-test.pdf`: `4.0M`

## Deleted Files and Directories

- `tsconfig.app.tsbuildinfo`
- `tsconfig.node.tsbuildinfo`
- `tools/content_lab/scripts/__pycache__/`

## Why Deletion Was Safe

- These files were ignored generated artifacts, not tracked product assets.
- Direct reference search for the filenames/stems returned no source, script, test, public, or docs references.
- TypeScript build-info files are regenerated by `tsc -b`.
- Python bytecode under `__pycache__` is interpreter cache and is not required by source scripts.
- Existing `.gitignore` already ignored `*.tsbuildinfo`, `__pycache__/`, and `*.pyc`; the tracked product surface does not depend on these files.

## Files and Directories Intentionally Kept

- `src/`, `tests/`, `scripts/`, package files, config files, README, and lockfiles.
- `public/assets/exam-bank-data/`, because it is the local exam-bank source mirror used by the asset sync/build path.
- `docs/assets/exam-bank-data/`, because it is the current GitHub Pages P3 image subset and is referenced by generated static pages.
- `asset-manifests/`, because it is part of the exam-bank restore/sync machinery.
- `tools/content_lab/outputs/`, including `review_queue.json`, because it supports current Content QA workflows.
- `content-model/` PDFs, because they appear to be source/reference course-content artifacts rather than legacy game/art assets.
- `.agents/skills/`, because these are agent operating resources, not deploy assets; deletion would be a separate repo hygiene decision.
- `.env` and `.env.live.local`, because they are ignored local environment files and may contain user-local configuration.

## Uncertain Files Needing User Decision

- `content-model/`: `36M` of PDFs. These are not deploy-critical, but they may be source references for course-content work. Move outside the repo or document ownership if they are no longer active.
- `docs/assets/exam-bank-data/`: `119M` after build. It is appropriate for the current GitHub Pages image-first deployment, but it is the main tracked static-output weight.
- `.git/`: `292M`. Git history is a major local size source. Cleaning it requires history analysis/rewrite decisions and should not be done in this direct cleanup pass.
- `.agents/skills/supabase*`: small, non-product agent assets. They do not affect deploy size materially, but they conflict thematically with the no-Supabase product direction unless intentionally kept for agent workflows.
- `.env` and `.env.live.local`: ignored local files. Keep, move, or delete only by user choice.

## `.gitignore` Gaps Found

Existing ignore coverage was already good for:

- `node_modules/`
- `.vite/`
- `dist/`
- logs and temp files
- `*.tsbuildinfo`
- `public/assets/exam-bank-data/`
- `__pycache__/`
- `*.pyc`

Gaps found for local visual/concept cleanup artifacts:

- local screenshots
- generated mockup folders
- local concept-art exports
- downloaded release-bundle folders
- design-source binaries

## `.gitignore` Changes Made

Added:

```gitignore
screenshots/
mockups/
concept-art/
release-bundles/
*.psd
*.ai
*.sketch
*.fig
*.blend
```

No image extensions such as `*.png`, `*.jpg`, `*.svg`, or `*.webp` were ignored, because those can be legitimate exam or static-site assets.

## Tracked Large Files Still Remaining

Tracked large files are mostly generated `docs/` deployment assets and source/reference PDFs:

- `docs/assets/exam-bank-data/`: `792` tracked P3 exam-bank files, `119M` after build.
- `content-model/P1/p1-content map.pdf`: `7.9M`
- `content-model/M1/m1-total.pdf`: `5.0M`
- `content-model/P3/integral-test.pdf`: `4.0M`
- Other `content-model/P1` and `content-model/P3` PDFs from about `516K` to `2.2M`.
- `tools/content_lab/outputs/review_queue.json`: `660K`

## Untracked Large Files Still Remaining

- `public/assets/exam-bank-data/`: `471M`, ignored, untracked local all-course exam-bank mirror.
- `node_modules/`: `113M`, ignored dependency install.
- `.env` and `.env.live.local`: ignored local environment files, not large.

## Bloat Classification

- Local working-tree bloat is mostly untracked/ignored assets and dependencies: `public/assets/exam-bank-data/` plus `node_modules/`.
- Tracked clone bloat is mostly generated static deployment output under `docs/assets/exam-bank-data/`, plus `content-model/` PDFs.
- `.git/` history is also large at `292M`.
- The cleanup did not find material legacy game/art asset bloat in the current tree.

## `docs/` Deployment Size

`docs/` is large but consistent with the current GitHub Pages static deployment model:

- `docs/`: `127M` after the required build.
- `docs/assets/exam-bank-data/`: `119M`.
- `docs/assets/exam-bank-data/` contains `792` tracked files, matching the deployed P3 image subset.

This is appropriate if GitHub Pages must serve image-first P3 question and mark-scheme assets directly from `docs/`. It is the right next optimization target if deploy size becomes painful.

## `public/assets/exam-bank-data/`

`public/assets/exam-bank-data/` is present and large:

- Size: `471M`
- Build verification reported `2607` files and `488,076,770` total bytes.
- `git ls-files public/assets/exam-bank-data` returned `0` files.
- It is ignored by `.gitignore`.

This is a local all-course source mirror, not tracked deploy output.

## Blunt Verdict

- Main source of bloat: local exam-bank assets in `public/assets/exam-bank-data/`, tracked P3 deploy assets in `docs/assets/exam-bank-data/`, dependencies in `node_modules/`, and Git history.
- What was safely deleted: ignored generated cache files only, specifically TypeScript build-info and Python bytecode cache.
- What should be cleaned next: decide whether `content-model/` PDFs belong in the repo, optimize or externalize tracked `docs/assets/exam-bank-data/`, and audit Git history if clone size matters.
- What should not be touched yet: canonical exam-bank data/assets, P3 deploy image pairs, current Content QA outputs, static build scripts, tests, and source code.
- Clean-clone friendly now: partially. The current tree has no obvious legacy game/art asset folders, but clone size is still affected by tracked `docs/`, `content-model/`, and `.git` history.
- Legacy game/art direction materially removed: no additional material removal was possible in this pass because obvious legacy game/art assets were not present. The repo already appears to have moved away from those assets in the current working tree.

## Recommended Next Cleanup Sequence

1. Decide whether `content-model/` PDFs are active source artifacts; move inactive PDFs to external storage or a separate reference archive.
2. Add a manifest-based check that `docs/assets/exam-bank-data/` includes only student-runtime-safe P3 image pairs needed by current static pages.
3. Run image optimization experiments on a copied `docs/assets/exam-bank-data/` tree and compare visual readability before changing deployed assets.
4. Audit `.git` history for large removed assets with a history tool before considering any rewrite.
5. Document the expected lifecycle for `public/assets/exam-bank-data/` as an ignored local restore/sync source.
6. Decide whether `.agents/skills/supabase*` belongs in the project repo or should be kept outside the product tree.

## Verification

- `npm test`: passed, 10 test files, 96 tests.
- `npm run build`: passed; asset sync verified `public/assets/exam-bank-data`, then generated 46 static HTML pages in `docs/`.
- `npm run static:check`: passed; static site check passed for 46 HTML pages, rendered static page check passed, and P3 Skill Check interaction browser check passed.
